import React from 'react';

import Header from './Header';

const Error = (props)=> {
    return(
        <div>
            <Header />
            <h1>Uh Oh! That was not found!</h1>
        </div>
    )
}

export default Error;